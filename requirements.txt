# =============================================================================
# AI Nutrition Assistant — Direct Dependencies Only
# Transitive deps (aiohttp, anyio, httpcore, etc.) resolved by pip automatically
# =============================================================================

# -- Core Framework --
fastapi>=0.115.12
uvicorn>=0.40.0
pydantic>=2.12.5
python-dotenv>=1.2.1

# -- LLM / AI --
pydantic-ai[anthropic]==1.39.0
anthropic==0.75.0
openai>=2.14.0
mem0ai>=1.0.5
langfuse>=3.0.0

# -- Database --
supabase>=2.15.0
vecs>=0.4.5

# -- HTTP / Networking --
httpx>=0.28.1

# -- Scientific / Math --
numpy>=1.26.4
scipy>=1.15.2

# -- Search / Parsing --
PyYAML>=6.0.2

# -- CLI / UI --
rich>=13.9.4
streamlit>=1.30.0
Pillow>=10.4.0
